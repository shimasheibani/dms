diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000..fc64c18
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,11 @@
+# Copy this file to .env and fill in your own values.
+# .env is gitignored and must never be committed.
+
+DB_USERNAME=root
+DB_PASSWORD=change-me
+
+# Must be a long random string (32+ chars) — used to sign JWTs.
+JWT_SECRET=change-me-to-a-long-random-secret
+
+# Only needed if the GitLab integration is actually used.
+GITLAB_TOKEN=change-me
diff --git a/.gitignore b/.gitignore
index 5ff6309..5c20d39 100644
--- a/.gitignore
+++ b/.gitignore
@@ -35,4 +35,6 @@ build/
 .vscode/
 
 ### Mac OS ###
-.DS_Store
\ No newline at end of file
+.DS_Store
+### Environment ###
+.env
diff --git a/src/main/java/org/dms/controller/AuthUser.java b/src/main/java/org/dms/controller/AuthUser.java
index 125fa59..b92d274 100644
--- a/src/main/java/org/dms/controller/AuthUser.java
+++ b/src/main/java/org/dms/controller/AuthUser.java
@@ -8,14 +8,14 @@ import org.dms.dto.Response;
 import org.dms.service.UserService;
 import org.springframework.http.ResponseEntity;
 import org.springframework.stereotype.Controller;
-import org.springframework.web.bind.annotation.GetMapping;
+import org.springframework.web.bind.annotation.PostMapping;
 import org.springframework.web.bind.annotation.RequestBody;
 import org.springframework.web.bind.annotation.RequestMapping;
 
 @Controller
 @Slf4j
 //@RequiredArgsConstructor
-@RequestMapping("/auth")
+@RequestMapping("/api/auth")
 public class AuthUser {
     private final UserService userService;
 
@@ -23,12 +23,12 @@ public class AuthUser {
         this.userService = userService;
     }
 
-    @GetMapping("/register")
+    @PostMapping("/register")
     public ResponseEntity<Response> register(@RequestBody RegisterRequest registerRequest) {
         return ResponseEntity.ok(userService.registerUser(registerRequest));
     }
 
-    @GetMapping("/login")
+    @PostMapping("/login")
     public ResponseEntity<Response> login(@RequestBody LoginRequest loginRequest) {
         return ResponseEntity.ok(userService.loginUser(loginRequest));
     }
diff --git a/src/main/java/org/dms/controller/UserController.java b/src/main/java/org/dms/controller/UserController.java
index 5b95e8b..632c3e6 100644
--- a/src/main/java/org/dms/controller/UserController.java
+++ b/src/main/java/org/dms/controller/UserController.java
@@ -8,11 +8,10 @@ import org.dms.dto.Response;
 import org.dms.dto.UsersDto;
 import org.dms.entity.Users;
 import org.dms.service.UserService;
-import org.springframework.context.annotation.Configuration;
 import org.springframework.http.ResponseEntity;
 import org.springframework.web.bind.annotation.*;
 
-@Configuration
+@RestController
 @RequestMapping("api/user")
 //@RequiredArgsConstructor
 @Slf4j
@@ -35,11 +34,11 @@ public class UserController {
     public ResponseEntity<Response> updateUser(@PathVariable Long id, @RequestBody @Valid UsersDto userDto){
         return ResponseEntity.ok(userService.updateUser(id, userDto));
     }
-    @DeleteMapping("/{id")
+    @DeleteMapping("/{id}")
     public ResponseEntity<Response> deleteUsr(@PathVariable Long id){
         return ResponseEntity.ok(userService.deleteUser(id));
     }
-    @GetMapping("/userDocument")
+    @GetMapping("/userDocument/{id}")
     public ResponseEntity<Response> getUserDocuments(@PathVariable Long id){
         return ResponseEntity.ok(userService.getUserDocuments(id));
     }
diff --git a/src/main/java/org/dms/security/AuthFilter.java b/src/main/java/org/dms/security/AuthFilter.java
index 0d38290..cb59746 100644
--- a/src/main/java/org/dms/security/AuthFilter.java
+++ b/src/main/java/org/dms/security/AuthFilter.java
@@ -26,20 +26,35 @@ public class AuthFilter extends OncePerRequestFilter {
     protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain) throws ServletException, IOException {
         String token = getTokenFromRequest(request);
         if(token != null){
-            String email = jwtUtils.getUsernamFromToken(token);
-            UserDetails userDetails = customUserDetailsService.loadUserByUsername(email);
-            if(StringUtils.hasText(email) && jwtUtils.isTokenValid(token, userDetails)){
-                log.info("Token is valid, for username of " );
-                UsernamePasswordAuthenticationToken authenticationToken = new UsernamePasswordAuthenticationToken(userDetails, null, userDetails.getAuthorities());
-                authenticationToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
-                SecurityContextHolder.getContext().setAuthentication(authenticationToken);
-                try{
-                    filterChain.doFilter(request,response);
-                }catch (Exception ex){
-                    log.error("Exception occured while logging in");
+            try {
+                String email = jwtUtils.getUsernamFromToken(token);
+                if(StringUtils.hasText(email)) {
+                    UserDetails userDetails = customUserDetailsService.loadUserByUsername(email);
+                    if(jwtUtils.isTokenValid(token, userDetails)){
+                        log.info("Token is valid, for username of {}", email);
+                        UsernamePasswordAuthenticationToken authenticationToken = new UsernamePasswordAuthenticationToken(userDetails, null, userDetails.getAuthorities());
+                        authenticationToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
+                        SecurityContextHolder.getContext().setAuthentication(authenticationToken);
+                    }
                 }
+            } catch (Exception e) {
+                // Invalid, malformed, or expired token: leave the security context empty
+                // so the request falls through as unauthenticated instead of never
+                // reaching filterChain.doFilter() below.
+                log.warn("Rejected invalid JWT: {}", e.getMessage());
+                SecurityContextHolder.clearContext();
             }
         }
+        // This must run unconditionally: previously it only ran inside the
+        // "token present and valid" branch, so any request with no token or
+        // an invalid token never continued down the filter chain at all
+        // (the request would just hang instead of reaching the controller
+        // or a 401 response).
+        try {
+            filterChain.doFilter(request, response);
+        } catch (Exception ex) {
+            log.error("Exception occurred while processing request", ex);
+        }
     }
 
     private String getTokenFromRequest(HttpServletRequest request) {
diff --git a/src/main/java/org/dms/service/impl/DocumentServiceImp.java b/src/main/java/org/dms/service/impl/DocumentServiceImp.java
index 7028d6b..ba33283 100644
--- a/src/main/java/org/dms/service/impl/DocumentServiceImp.java
+++ b/src/main/java/org/dms/service/impl/DocumentServiceImp.java
@@ -90,7 +90,10 @@ public class DocumentServiceImp implements DocumentService {
             documents.setDocumentUrl(documentPath);
         }
         documentRepository.save(documents);
-        return null;
+        return Response.builder()
+                .status(200)
+                .message("Document updated successfully")
+                .build();
     }
 
     @Override
diff --git a/src/main/java/org/dms/service/impl/UserServiceImpl.java b/src/main/java/org/dms/service/impl/UserServiceImpl.java
index e95e773..3deb166 100644
--- a/src/main/java/org/dms/service/impl/UserServiceImpl.java
+++ b/src/main/java/org/dms/service/impl/UserServiceImpl.java
@@ -71,7 +71,7 @@ public class UserServiceImpl implements UserService {
     @Override
     public Response getAllUser() {
         List<Users> usersList = userRepository.findAll(Sort.by(Sort.Direction.DESC,"id"));
-        List<UsersDto> userDtoList = modelmapper.map(usersList, new TypeToken<UsersDto>() {}.getType());
+        List<UsersDto> userDtoList = modelmapper.map(usersList, new TypeToken<List<UsersDto>>() {}.getType());
         return Response.builder()
                 .status(200)
                 .message("All Users List Is as below:")
diff --git a/src/main/resources/application.properties b/src/main/resources/application.properties
index bf9027c..cb51eef 100644
--- a/src/main/resources/application.properties
+++ b/src/main/resources/application.properties
@@ -1,26 +1,17 @@
 server.port=8081
 
 spring.datasource.url=jdbc:mysql://localhost:3306/DMS_database
-<<<<<<< HEAD
-
-=======
->>>>>>> bee2c3d2fefd102ba4b57642e6b421d97e0ffd8c
-spring.datasource.username=root
-spring.datasource.password=Aa!123456
+spring.datasource.username=${DB_USERNAME:root}
+spring.datasource.password=${DB_PASSWORD}
 spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 
 spring.jpa.hibernate.ddl-auto=update
 spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
 
-<<<<<<< HEAD
-
-
-=======
->>>>>>> bee2c3d2fefd102ba4b57642e6b421d97e0ffd8c
 gitlab.url=http://localhost:8080
-gitlab.token=glpat-puK2menyQwHen17a4MyF
-secretJwtString= "qweASDzxcP:>123146(*&dfe"
+gitlab.token=${GITLAB_TOKEN}
+secretJwtString=${JWT_SECRET}
 
 spring.servlet.multipart.enabled=true
 spring.servlet.multipart.max-file-size=10MB
-spring.servlet.multipart.max-request-size=2MB
\ No newline at end of file
+spring.servlet.multipart.max-request-size=20MB
